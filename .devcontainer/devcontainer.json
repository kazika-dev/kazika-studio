{
  "name": "kazika-studio",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:1-22-bookworm",
 
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "lts",
      "nodeGypDependencies": true
    }
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode"
      ]
    }
  },


 "postCreateCommand": "sudo mkdir -p /home/node/.ssh /home/node/.config/vibe-kanban && sudo chown -R node:node /home/node/.ssh /home/node/.config/vibe-kanban && chmod 700 /home/node/.ssh && chmod 755 /home/node/.config/vibe-kanban && ssh-keyscan github.com >> /home/node/.ssh/known_hosts && chmod 644 /home/node/.ssh/known_hosts && npm install && npm install -g @anthropic-ai/claude-code",
"remoteUser": "node",

  "mounts": [
    "source=kazika-studio-ssh,target=/home/node/.ssh,type=volume",
    "source=kazika-studio-vibe-kanban,target=/home/node/.config/vibe-kanban,type=volume"
  ],

  "forwardPorts": [11001]
}
